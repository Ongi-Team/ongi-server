spring:
  application:
    name: ongi

  profiles:
    default: dev

  jackson:
    serialization:
      write-dates-as-timestamps: false

springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: method